# hgit-native

hgit for Windows, macOS and Linux: a native Go port of the TempleOS version-control system.
Same `.hgs` format and commands as [hgit](https://github.com/VectorSophie/hgit); the format spec and golden fixtures come from the pinned `contract/` submodule (`contract-1.8.9`).

Work in progress.
